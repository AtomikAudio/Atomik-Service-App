# Dependencies
node_modules/

# Environment & secrets
.env
.env.local
.env.*.local
rzp-key.csv
*-key.csv
backend/dist/

# Local-only staff/production credentials (NEVER commit)
*.local.md
STAFF_CREDENTIALS.local.md
backend/STAFF_CREDENTIALS.local.md

# Expo / React Native
.expo/
.expo-tmp-bundle/
dist/
web-build/
google-service-account.json
frontend/google-service-account.json
# Firebase Android client config — local + EAS file env only (never commit)
frontend/google-services.json
google-services.json
# Firebase Admin / FCM V1 private keys (never commit — upload to Expo credentials only)
*firebase-adminsdk*.json
credentials.json
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Native builds
android/app/build/
ios/build/
*.ipa
*.apk

# OS & IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Metro
.metro-health-check*

# TypeScript
*.tsbuildinfo

# Misc
coverage/
.cache/
frontend/releases/
*.aab
*.apk
ATOMIK_Technician_Guide.pptx
ServiceAppGuide/
append_slides.py
build_ppt.py
